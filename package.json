{
    "private": true,
    "scripts": {
        "clean": "rimraf output .pulp-cache",
        "build": "pulp build -- --censor-lib --strict",
        "example": "pulp build -O -I example/src --to example/dist/app.js",
        "docs":
            "purs docs --format html \"src/**/*.purs\" \"bower_components/*/src/**/*.purs\""
    },
    "dependencies": {
        "pulp": "^12.0.3",
        "purescript": "^0.12",
        "purescript-psa": "^0.7.3"
    },
    "devDependencies": {
        "rimraf": "^2.6.2"
    }
}
